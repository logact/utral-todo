# This file is now deprecated.
# Nginx is integrated into the main container via the Dockerfile.
# Use docker-compose.yml instead, which exposes port 80 internally.
#
# For external nginx reverse proxy, use the standard docker-compose.yml
# and configure your external nginx to proxy to the container's port 80.
