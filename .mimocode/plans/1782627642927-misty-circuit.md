# Plan: Unify root package.json script names

## Goal
Rewrite root `scripts` in `/Users/logact/projects/utral-todo/package.json` to follow a consistent `(app/function):(action)` naming convention.

## Naming convention
- **app:function** pattern: `server:dev`, `server:build`, `cli:build`, `expo:dev`, `desktop:dev`, `desktop:build`
- **function:action** pattern for cross-cutting: `db:generate`, `db:migrate`, `db:push`, `db:studio`, `lint`, `typecheck`, `clean`
- **Action variants**: `:ios`, `:all` appended where needed (e.g. `expo:dev:ios`, `dev:all`)

## Current → New mapping

| Current | New | Change? |
|---|---|---|
| `server:dev` | `server:dev` | no |
| `dev:all` | `dev:all` | no (meta command) |
| `dev:expo` | `expo:dev` | **yes** |
| `dev:expo:ios` | `expo:dev:ios` | **yes** |
| `build:expo` | `expo:build` | **yes** |
| `dev:desktop` | `desktop:dev` | **yes** |
| `build:dev` | `desktop:build` | **yes** |
| `build:server` | `server:build` | **yes** |
| `build:cli` | `cli:build` | **yes** |
| `db:generate` | `db:generate` | no |
| `db:migrate` | `db:migrate` | no |
| `db:push` | `db:push` | no |
| `db:studio` | `db:studio` | no |
| `lint` | `lint` | no |
| `typecheck` | `typecheck` | no |
| `clean` | `clean` | no |
| `cli` | `cli` | no (utility) |

## File to modify
- `/Users/logact/projects/utral-todo/package.json` (root scripts section only)

## Additional fix
- `dev:all` currently references `pnpm dev:server` (doesn't exist, should be `server:dev`) and `pnpm dev:desktop` (renamed to `desktop:dev`). Update to correct names.

## Verification
- `pnpm run --help` or inspect the rewritten JSON to confirm valid format
- Verify `dev:all` correctly chains the renamed scripts
